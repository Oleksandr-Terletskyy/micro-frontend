import { Routes } from '@angular/router';
import { FindingsListComponent } from './components/findings-list/findings-list.component';

export const routes: Routes = [
  { path: '', component: FindingsListComponent }
];